// Native and Platform Components
import { View, TouchableOpacity, Platform, ScrollView, Animated, PanResponder, Modal as LegacyModal, Image } from 'react-native'


// // Native Paper Components
import { Provider as StyleProvider, Appbar, Avatar, Menu, Divider, Badge, RadioButton, Snackbar, Portal, ProgressBar, Checkbox, DefaultTheme, ActivityIndicator } from './paper';

// // Custom Components
import './platform'
import TextEditor from './TextEditor'
import Drawer from './Drawer'
import Modal from './Modal'
import { Card, Chip, List, Surface } from './Material'
import { createGrid, Container, Section, Area } from './Layout'
import { Button, BubbleButton, ToggleButton } from './Button'
import { Text, Title, Heading, Subheading, Paragraph, Caption } from './Typography'
import { TextInput, PasswordInput, DateInput, TextField, HelperText, ToggleSwitch, FileInput, ToggleInput, DateTimeInput, TextArea } from './Form'
import { Route, Redirect, Switch } from '@triframe/arbiter-react';
import { Provider, tether } from './Provider';
import { Icon } from './Icon'
import { Tab } from './Tabs'

const { Grid, Column } = createGrid({
    xs: 0,
    sm: 350,
    md: 450,
    lg: 650,
    xl: 950
})

export {
    ActivityIndicator,

    Provider,

    tether,

    // Navigation
    Route,  Appbar, Drawer, Redirect,

    // Layout
    View, Container, Section, Area, Grid, Column, Divider, 

    // Typography
    Text, Title, Heading, Subheading, Paragraph, Caption, Icon,
    
    // Popups
    Modal, Menu, Snackbar,

    // Inputs
    TextInput, PasswordInput, FileInput, ToggleInput, DateInput, DateTimeInput, TextArea, TextField, HelperText, ToggleSwitch, RadioButton, Checkbox,

    // Buttons
    Button, BubbleButton, ToggleButton,

    // Material
    Card, Chip, Badge, Avatar, Surface, List, Tab, // < Item, Accordion, AccordionGroup


    Switch,

    // Later
    DefaultTheme,  

    // Low Level

    TouchableOpacity, ScrollView, Animated, PanResponder, LegacyModal, Image, Platform, StyleProvider,

    // Depr

    Portal,  ProgressBar,

}