import React from 'react'
export const Icon = ({ name, color, size = {}, style = {}, ...props }) => (
    <span style={{ color, fontSize: size, ...style }} className={`mdi mdi-${name}`} {...props}></span>
)