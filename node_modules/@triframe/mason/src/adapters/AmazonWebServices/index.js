import { Repo, print, exec, fs } from "../../utils"

export const AmazonWebServices = {

    requiresRelease: false,

    getOptions: async () => {
        return {}
    },

    dependencyCheck: async () => {

    },

    deploy: async (branch, options) => {
        
    }

}