import inquirer from "inquirer";

export const prompt = async (question, choices) => {
    const { value } = await inquirer.prompt([{
        message: question,
        name: 'value',
        type: choices === undefined ? 'input' : 'list',
        choices
    }])
    return value
}