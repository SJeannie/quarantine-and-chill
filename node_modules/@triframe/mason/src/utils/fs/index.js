import { rmdir } from './rmdir'
import { copy } from './copy'

const fs = require('fs').promises

const exists = async (path) => {
    try {
        await fs.access(path)
        return true
    } catch {
        return false
    }
}

Object.assign(fs, { rmdir, copy, exists })


export { fs }