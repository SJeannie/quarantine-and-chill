"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
const isWindows = process.platform === 'win32' || /^(msys|cygwin)$/.test(process.env.OSTYPE);

class EnvironmentVariables {
  toString() {
    let variables = Object.keys(this);

    let setVariable = variable => isWindows ? `set ${variable}=${this[variable]} &&` : `${variable}=${this[variable]}`;

    return variables.reduce((string, variable) => `${string}${setVariable(variable)} `, '');
  }

}

exports.EnvironmentVariables = EnvironmentVariables;
//# sourceMappingURL=EnvironmentVariables.js.map