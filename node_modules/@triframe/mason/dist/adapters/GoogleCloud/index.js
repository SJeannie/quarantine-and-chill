"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.GoogleCloud = undefined;

var _utils = require("../../utils");

const GoogleCloud = exports.GoogleCloud = {
  requiresRelease: true,
  dependencyCheck: async () => {},
  getOptions: async () => {
    let {
      name
    } = await (0, _utils.getState)();
    let gcProjectName = await (0, _utils.prompt)(`What would you like to name the Google Cloud project? A new project with this name will be created if none already exists (${name})`);
    let gcRegion = await (0, _utils.prompt)(`What region would you like to deploy to? (us-central1)`);
    if (gcProjectName == '') gcProjectName = name;
    if (gcRegion == '') gcRegion = 'us-central1';
    return {
      gcProjectName,
      gcRegion
    };
  }
};
//# sourceMappingURL=index.js.map