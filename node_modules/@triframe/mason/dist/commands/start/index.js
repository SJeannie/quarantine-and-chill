"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _utils = require("../../utils");

var _startApi = require("./startApi");

var _startApp = require("./startApp");

exports.default = async () => {
  let envPath = _utils.path.join(process.cwd(), '.env');

  if (!(await _utils.fs.exists(envPath))) {
    await _utils.fs.writeFile(envPath, `
# DATABASE_URL=postgresql://username:password@localhost:5432/name
API_DEV_PORT=8080
APP_DEV_PORT=8081
        `);
  }

  (0, _startApi.startApi)();
  (0, _startApp.startApp)();
};
//# sourceMappingURL=index.js.map