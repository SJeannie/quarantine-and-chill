import path from 'path'

export const frontendHandler = (req, res) => {
    res.sendFile(path.resolve('./public/index.html'))
}

export const socketHandler = (socket, schema) => {

    socket.on('*', (payload, send, event) => schema.emit(event, { ...payload, socket, send }))

    socket.emit('install', schema)

}