"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

const initializeServer = exports.initializeServer = (server, port) => new Promise(resolve => server.listen(port, resolve));

const initializeDatabase = exports.initializeDatabase = async databaseUrl => {
  let connectDB;

  try {
    ({
      connectDB
    } = require('@triframe/scribe'));
  } catch {
    throw Error(`Provided databaseUrl '${databaseUrl}' but '@triframe/scribe' could not be found (Install it, or don't pass databaseUrl to serve)`);
  }

  return await connectDB(databaseUrl);
};
//# sourceMappingURL=initializers.js.map