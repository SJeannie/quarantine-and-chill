"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

const when = exports.when = (condition, result, ...alternatives) => {
  if (condition) return result();else {
    let alternative = alternatives.find(alternative => alternative.predicate === true);
    if (alternative) return alternative.result();else return null;
  }
};

const otherwiseWhen = exports.otherwiseWhen = (predicate, result) => {
  return {
    predicate,
    result
  };
};

const otherwise = exports.otherwise = result => {
  return {
    predicate: true,
    result
  };
};

let invoke = exports.invoke = (func, ...args) => typeof func === 'function' && func(...args);

let sleep = exports.sleep = time => new Promise(resolve => setTimeout(resolve, time));
//# sourceMappingURL=index.js.map